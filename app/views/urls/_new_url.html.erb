<%= form_for(@url, remote: true) do |f| %>
  <%= render '/shared/error_messages', object: @url %>
  <!-- <%= error_messages_for(@url) %> -->
  <%= f.text_field :target_url, placeholder: "Enter an URL" %>

  <%= f.submit "Create" %>
<% end %>
